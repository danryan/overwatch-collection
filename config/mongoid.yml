# defaults: &defaults
  # parameterize_keys: true

development:
  host: localhost
  database: overwatch_collection_development
  # <<: *defaults
  
test:
  host: localhost
  database: overwatch_collection_test

# set these environment variables on your prod server
production:
  host: <%= ENV['MONGOID_HOST'] %>
  port: <%= ENV['MONGOID_PORT'] %>
  username: <%= ENV['MONGOID_USERNAME'] %>
  password: <%= ENV['MONGOID_PASSWORD'] %>
  database: <%= ENV['MONGOID_DATABASE'] %>
  # slaves:
  #   - host: slave1.local
  #     port: 27018
  #   - host: slave2.local
  #     port: 27019
parameterize_keys: true
